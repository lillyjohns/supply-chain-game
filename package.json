{
  "name": "supply-chain-game",
  "version": "1.0.0",
  "description": "Supply Chain Broker - Multiplayer turn-based logistics board game",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js",
    "build": "cd client && npm run build && cp -r dist ../public",
    "install-all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "cors": "^2.8.5"
  }
}
